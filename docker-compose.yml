version: "3.8"

services:
  sora-playlist:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - ./logs:/app/logs
    working_dir: /app
    command: python app.py
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
