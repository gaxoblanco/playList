<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirigir todas las solicitudes no existentes al index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
